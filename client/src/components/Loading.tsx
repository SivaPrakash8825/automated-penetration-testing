import React from "react";

type Props = {};

const Loading = (props: Props) => {
  return (
    <div className=" w-6 h-6 border-b-2 border-l-2 rounded-full  border-white animate-spin"></div>
  );
};

export default Loading;
